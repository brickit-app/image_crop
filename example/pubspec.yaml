name: image_crop_example
description: Demonstrates how to use the image_crop plugin.
repository: https://github.com/brickit-app/country-list-pick.git

version: 1.1.0+2

environment:
  sdk: ^3.10.8
  flutter: ^3.41.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.5
  image_picker: ^1.1.2
  image_crop:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
